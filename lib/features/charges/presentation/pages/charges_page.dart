import 'package:flutter/material.dart';

class ChargesPage extends StatelessWidget {
  const ChargesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Cobranças (Em breve)'),
      ),
    );
  }
}
